<!DOCTYPE html>
<html>
  <head>
    <title>AirbnbClone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body 
    data-user-properties="<%= request.path == '/user_properties' %>"
    data-booking-confirmation="<%= request.path.start_with?('/booking_confirmation') %>"
    >
    <%= yield %>
  </body>
</html>
